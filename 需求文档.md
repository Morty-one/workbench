# 个人工作台（workbench-REPLICA）需求文档

> 用途：把**当前已经做好的功能**整理成需求快照，供逐条审阅。
> 你（用户）将据此判断：哪些模块间的**逻辑关联是不必要的**、哪些需要**变更**。
> 本文档同时单列「视觉与交互风格」一节，并在其中**标注你不喜欢的 / 反复强调的 / 待你定夺的**点。
>
> 最后两节是**「待审视：可能多余或可疑的逻辑关联」**和**「当前已知约束与边界」**，可直接作为取舍清单。

---

## 1. 系统定位与技术架构

**定位**：本地优先（local-first）的个人轻量工作台，纯前端（Vue3 + Vite），数据存浏览器 IndexedDB，可离线使用、可"安装到桌面"（PWA）。

**技术栈**
- 前端：Vue 3（`<script setup>` 单文件组件）、Vite 5
- 本地数据库：Dexie（IndexedDB 封装），无后端服务器
- 加密：Web Crypto（AES-GCM + PBKDF2）
- 本地桥接：Node 进程 `local-bridge.cjs`（端口 4567）+ PowerShell，用于唤起本机软件（Excel/WPS）与本地文件
- 云端同步：GitHub Contents API（私有库加密快照）
- 构建产物目录：`dist2`（`vite.config.js` 设 `outDir:'dist2'`、`emptyOutDir:false`）

**启动链路**
双击 `打开工作台.vbs` → 启动 `local-bridge.cjs`（Node，端口 4567）→ 用 `vite preview` 服务 `dist2`（端口 4173，严格绑定）→ 打开浏览器。

---

## 2. 数据模型（Dexie 表，版本 v1→v4 演进）

| 表 | 关键字段 | 说明 |
|----|---------|------|
| `tasks` | id, title, quadrant（四象限）, status, projectId, followUpAt, nextRemindAt, completedAt, subtasks[], docOutput, remark | 任务；`projectId` 在 v3 引入 |
| `folders` | id, name, parentId | 知识库目录树 |
| `notes` | id, title, type, folderId, tags, content(markdown), meetingTime, attendees | 笔记/速记/会议记录 |
| `shortcuts` | id, name, url, icon | 快捷入口（总览页渲染+编辑） |
| `duty` | id, date, person, shift, remark, workContent | 值班/排班记录 |
| `settings` | key | 键值配置（皮肤、同步、自动化规则、预设等都在此） |
| `projects` | id, name, color, parentId, progressMode(auto/manual), manualProgress, order, archived, startAt, endAt | v3 引入；支持嵌套父项目（v4 parentId） |
| `handles` | id | 用户选定的本地目录句柄（FileSystemDirectoryHandle，仅 Chromium） |

**升级迁移**：v3 升级时自动建「默认项目」并把历史任务挂进去；老数据不丢。

---

## 3. 功能模块清单（按左侧导航）

### 3.1 总览 Overview（`总览`）
- 顶部问候条（hero）：日期问候语 + 今日待处理数（含逾期数徽标）+ 今日/明日值班摘要（可点「日程」跳 Duty、「转天」切换，「转天」切换后，「转天」按钮变更为「今日」按钮，点击返回今日值班员信息）。
- 统计卡（stat-row）：4 张 KPI（今日待办 / 进行中 / 已完成 / 逾期），带"较昨日"趋势。
- **今天要处理**（ov-grid 左/右，见视觉节）：今日任务清单，支持单任务完成、子任务勾选、延期（snooze）。
- **项目文档**（ProjectStack 3D 卡片堆叠）：展示 projects 完成度，可点开管理（ProjectManager 弹窗）。
- **完成趋势 + 最近笔记**（ov-grid3 两栏）。
- **快捷入口**：shortcuts 渲染 + 编辑（增删改）。

### 3.2 任务管理 Tasks（`任务管理`）
- 四象限（urgent-important 等）分组展示；按日筛选（今天/昨天/全部/指定日/范围）。
- 任务增删改、子任务、跟进提醒（followUpAt / nextRemindAt）。
- **项目筛选**：可由总览点击项目下钻进入（`tasksProjectFilter`）。
- 项目树拖拽（onProjDrop 改 projectId 的 parentId）。
- 值班预设联动：可把"值班任务"按规则生成（见 §4.3）。

### 3.3 复盘 Review（`复盘`）
- 顶部统计：今日新建/完成/进行中/逾期。
- **AI 智能复盘**：接 LLM（deepseek / ollama / 自定义），把今日任务+笔记上下文发给模型生成复盘文本，可回填到「每日梳理」。
- 今日任务、今日笔记、每日梳理三块。

### 3.4 知识库 Notes（`知识库`）
- 目录树（FolderTree）+ 笔记列表；markdown 编辑 + 预览（marked）。
- 笔记类型（flash 速记 / meeting 会议）+ 标签（可预设）。
- **笔记一键转为任务**（wb:goto 跳到任务管理）。
- 批量删除、勾选转任务。

### 3.5 日程管理 Duty（`日程管理`）
- 月历视图（可切换人数 2/3 模板）、按人员/班次/范围筛选。
- 排班编辑（添加/编辑/清空）、值班统计。
- 班次判定逻辑（`shift.js`）：主班 9:00–次日9:00 / 副班 9:00–18:00 / 周末白班 9:00–20:30（仅周末成立）/ 休班。
- 可视化：班次色块（`colorMap`）。

### 3.6 文档输出 DocOutput（`文档输出`）
- 一次性配置：A 文件（每次执行时选）、B 文件（固定路径）、复制改名、输出目录、删除 sheet、WPS 线上表链接、TXT 中转目录、**宏①/宏②名称与加载项**、Sheet 映射（本地名↔线上名、源范围↔目标范围）。
- 执行：选 A → 调本地桥 → PowerShell 驱动 Excel/WPS 跑宏 → 轮询状态 → 结果弹窗（分步诊断）。
- 关联预设任务说明、执行状态、执行记录（可导出 CSV）。

### 3.7 设置中心 Data（`设置中心`）
- 顶层 TAB：数据管理 / 预设。
- 预设子 TAB：任务预设 / 知识库预设 / 自动化 / 外观。
- **数据管理**：本地目录同步（FileSystem Access）、加密开关、云端同步（GitHub 仓库/PAT/密码）。
- **自动化**：值班/任务生成规则（见 §4.3）。
- **外观**：皮肤选择 + 强调色（浅色）+ clean 皮肤「重置为初始样式」。

---

## 4. 模块间的连接与逻辑（重点）

### 4.1 导航与下钻
- `App.vue` 持有 `current`（当前视图）+ `tasksProjectFilter`。
- 总览点击项目卡片 → `goto('tasks', {projectId})` → 任务管理按项目筛选。
- 总览值班按钮 → `goto('duty')`。
- 笔记转任务 → `window.dispatchEvent(new CustomEvent('wb:goto', {detail:{key:'tasks'}}))`，App 监听后 `goto`。
  - ⚠️ 这是**跨组件用全局事件总线（CustomEvent）通信**，而非 props/路由。轻量但隐式，排查时不易追踪来源（见 §7）。

### 4.2 笔记 → 任务
- Notes 选中笔记 → 调 `wb:goto` → 跳任务管理并进入新建（实际是把笔记标题/内容带过去）。

### 4.3 自动化规则 → 任务生成（含文档输出联动）
- 配置入口：设置中心 → 预设 → 自动化（`newRule*` 一堆字段：执行人、周几、班次 multi、日期范围、完成时间、提醒时间、关联文档输出、象限、备注、关联子任务）。
- 生成逻辑：`Overview.ensurePeriodicTasks()` 用 `matchShifts()` 按"日期范围 + 周几 + 班次"匹配，`migrateRuleForGen()` 兼容新旧字段，生成的任务落到 `tasks` 表（remark 含 `自动生成 · 人 · 班次`）。
- 文档输出联动：规则勾选 `docOutput` → 生成的任务到"完成时间"后，在 App 的提醒 toast 里出现「执行文档输出」按钮 → 触发 `requestDocOutput()` → 走 §4.5 管线。

### 4.4 数据同步（双写）
- **本地目录**（PC，Chromium）：`autosync.js` 监听 Dexie 中间件 `mutate` → 防抖 600ms → 写 `workbench-data.json`（明文）或 `workbench-data-encrypted.json`（AES-GCM，密码本地存）。
- **云端**（GitHub）：`cloudsync.js` 在本地变更后防抖 3s 推送加密快照；启动时先 pull，远端较新则整库还原并刷新页面（LWW 最后写入覆盖）。
- 本机专属配置键（`cloudRepo/cloudPat/cloudPw/syncPassword/autoSync*`）还原时**保留本端**、不跨端覆盖。
- ⚠️ **两套存储并存**：皮肤/强调色/cleanReset 存 `localStorage`；业务数据存 IndexedDB；同步配置既存 `settings` 表（IndexedDB）又镜像到 `localStorage`（`LS_SYNCED_AT`/`LS_DIRTY_AT`）。清理/迁移时易遗漏（见 §7）。

### 4.5 文档输出管线（前端 → 本机）
```
用户点执行 → DocOutput/Toast 调 requestDocOutput()
  → 选 A 文件（fetch 127.0.0.1:4567/pick-file）
  → POST /doc-output（aPath,bPath,config）→ local-bridge.cjs
  → 桥调 PowerShell 脚本 → 驱动 Excel/WPS 跑宏①② → 复制改名 → 写 WPS 线上表
  → 前端每 2s 轮询 /doc-output-status → 完成弹窗（分步 ok/fail + 诊断信息）
```
- 跨视图共享状态：`docState`（reactive，模块级），切走再切回不丢；`RUN_FLAG_KEY` 持久化，刷新可恢复轮询。
- 失败诊断：桥路径/mtime 检测"旧桥残留"（非 REPLICA 目录的桥进程），并给手动粘贴路径兜底。

### 4.6 提醒系统（toast + 桌面通知）
- `App.vue` 每 30s 扫 `tasks`：父任务或子任务到达 `remindTime` → 弹提醒 toast + 桌面通知；到达 `dueTime` → 弹"是否已完成"确认（文档输出类任务改为提示"执行文档输出"，不弹完成确认）。
- `primeReminded()` 启动时预置已过的提醒不再重复弹。
- snooze（延期）写回 `nextRemindAt`；完成写 `completedAt` 并广播 `task-updated` 让各视图刷新。

---

## 5. 视觉与交互风格（含「你不喜欢的」标注）

### 5.1 整体布局
- **三栏**：左侧导航（sidebar）/ 中部主内容（main）/ 右侧指标卡（metrics，240px）。
- 最大宽度 1600px 居中；≤1100px 隐藏右栏；≤720px 侧栏变底部 tab 栏（移动端）。

### 5.2 侧栏折叠
- 默认**折叠为细条（64px，仅图标）**；鼠标移到侧栏右边缘浮现圆形 ⌄/⌃ 按钮，点击展开/收起；状态存 `localStorage`（`wb_sidebar_collapsed`，默认 `'1'`）。
- 底部仅剩「换肤」按钮（原独立的深/浅月日切换已合并移除）。

### 5.3 皮肤系统（7 个）
清单（`App.vue` SKINS）：

| key | 标签 | 结构主题 | 调色板 |
|-----|------|---------|--------|
| `dark` | 深色 | dark | —（默认，不设 data-skin） |
| `light` | 浅色 | light | —（默认，不设 data-skin） |
| `glass` | 暗色玻璃 | dark | data-skin 叠加 |
| `espresso` | 暖棕 | dark | data-skin 叠加 |
| `aurora` | 极光 | dark | data-skin 叠加 |
| `clean` | 极简浅色 | light | data-skin 叠加 |
| `warm` | 暖白 | light | data-skin 叠加 |

**🔒 你不喜欢的 / 反复强调的（务必保留此约束）**
- **深色、浅色这两个默认皮肤绝对不要改**。任何皮肤相关的调整都必须隔离到其余 5 个，且通过 `data-skin` 叠加实现，确保 dark/light 行为 100% 不变。
- 早期 5 个新皮肤（v1/v2）你评价「**差别不大、不像草图那种质感**」→ 已按草图重做多轮（glass/espresso/aurora 在 dark 上做真质感，clean/warm 在 light 上做）。你对"质感"要求高，后续调皮肤建议先出草图再落地。
- **clean 皮肤**要求对照 2026-08-22 草图（极简浅白 + 柔和紫光），且必须支持「**一键回退到调整前**」（`wb_clean_reset` + `clean-old` class）。

**沟通原则（来自一次误解，记录在此防再犯）**
- 你举"把今天要处理挪到右边"只是**举例说明方向**，不是字面指令。真实意图是"参考美观的浅色 dashboard 自主重新设计布局"。→ 以后你给例子时，我应理解为方向而非逐字执行。

### 5.4 强调色
- 仅在**浅色主题**生效（默认蓝 ocean）；暗色皮肤下不写入 `data-accent`，由皮肤自带配色驱动。
- 5 色：海洋/翡翠/鸢尾/琥珀/樱粉。色板 UI 仅浅色皮肤显示（`v-if="isLightSkin"`）。

### 5.5 clean 皮肤的三视图 Bento 重排（最新实现，待你审阅）
- 仅 `[data-skin="clean"]` 前缀生效；其余 6 个皮肤 0 改动。
- 技术：12 列 grid + 中间层容器 `display:contents` 拍平，卡片直接参与 Bento 排布；回退用 `<html>` 上 `clean-old` class（比 prop 传递更干净，三视图统一生效）。
- 排布：
  - **总览**：问候条通栏 → 4 KPI 各 3 列 → 今天要处理 8 列(hero) + 项目文档 4 列 → 完成趋势 7 + 最近笔记 5 → 快捷入口通栏。
  - **复盘**：标题通栏 → KPI×3 → AI 复盘 5 + 今日任务 7 → 今日笔记 5 + 每日梳理 7。
  - **文档输出**：从 960px 窄列变全宽 → 关联说明 4 + 文件 8 → 基础配置 7 + Sheet 映射 5 → 执行按钮通栏渐变大卡 → 状态 5 + 记录 7。
- 「重置为初始样式」按钮现在同时回退**配色 token + 整套布局**。
- ⚠️ 此版你**尚未审阅**（刚做完）。如果你不喜欢 Bento 方向，点「重置为初始样式」可整体回退到调整前布局。

### 5.6 卡片"材质"实验（待你定夺）
- 指标卡/统计卡上有 `:data-material="['cyan','original','rain','chrome'][idx%4]"`，是早期为指标卡做的**4 种循环材质风格**实验。
- ⚠️ 这套循环材质与 clean 极简风格的"统一克制"调性可能冲突；是否保留、或在 clean 皮肤下关闭，待你定夺。

---

## 6. 待审视：可能多余或可疑的逻辑关联（直接服务你的取舍）

> 这一节是你最可能要动刀的地方。每条都标了"现象 / 影响 / 建议"。

1. **Dashboard.vue 是孤儿视图（死代码）**
   - 现象：不在左侧导航、不被任何组件 `import`/`goto`（仅 `db.js`/`seed.js` 注释提到）。它内部有时钟面板、VoiceInput、快捷入口管理。
   - 影响：它的"快捷入口管理"与总览页的 shortcuts 编辑**功能重复**；时钟/VoiceInput 是它独有的，但无人能访问到。
   - 建议：要么补一个导航入口把它用起来，要么整体删除（shortcuts 编辑在总览已有，不丢功能）。

2. **根目录遗留 JSON（doc-output-job.json / doc-output-result.json）**
   - 现象：是**旧 PS1 文档输出管线**的产物，现版 `docoutput.js` 改用 `localStorage` 日志，这两个文件已不被读取。
   - 建议：删除，避免和现版日志体系混淆。

3. **localStorage ↔ IndexedDB 双轨存储**
   - 现象：皮肤/强调色/cleanReset 在 localStorage；业务数据在 IndexedDB；云端同步配置既在 `settings` 表又在 localStorage（`LS_SYNCED_AT`/`LS_DIRTY_AT`/`cloudConfigured` 从 settings 读后写内存）。
   - 影响：清理或迁移时容易只清一处导致状态漂移。
   - 建议：能进 IndexedDB 的尽量统一进 `settings` 表（皮肤等也可存 settings），减少双轨。

4. **CLEAN_BEFORE 与 style.css 的 clean 块重复维护**
   - 现象：`App.vue` 里硬编码一份 clean 皮肤的"初始 token"常量（`CLEAN_BEFORE`），与 `style.css` 的 `[data-skin="clean"]` 块是**两份真相**。
   - 影响：若只改 style.css 的 clean 块却忘改 CLEAN_BEFORE，"重置为初始样式"回到的就不是真·初始。
   - 建议：回退基准改为从 style.css 读取，或把 clean 初始 token 也收进单一来源。

5. **全局事件总线（CustomEvent 'wb:goto'）**
   - 现象：笔记转任务、导航跳转用 `window.dispatchEvent` 隐式通信。
   - 影响：跨组件、隐式、难追踪来源与调试。
   - 建议：视图都在 App 内，可改为 props/emits 或集中 router 状态，更显式。

6. **ProjectStack 3D 卡片堆叠**
   - 现象：总览内嵌 `ProjectStack.vue`（projects 完成度 3D 卡片）。
   - 影响：是较重的可视化特性，与"极简"调性可能有距离。
   - 建议：确认是否保留；若做极简 clean 皮肤，3D 卡片在该皮肤下是否要降级为平面列表。

7. **指标卡 4 种循环材质（见 §5.6）**
   - 建议：clean 皮肤下是否关闭材质循环，统一为克制风格。

---

## 7. 当前已知约束与边界

- **本地桥依赖双击启动**：文档输出/本地软件唤起都靠 `local-bridge.cjs` 常驻；没双击 `打开工作台.vbs` 则桥未起，相关功能不可用。
- **本地目录同步仅 Chromium**（FileSystem Access API）；Firefox/Safari 不可用，自动降级为仅 IndexedDB。
- **桌面通知仅在页面打开时弹出**；关浏览器后提醒失效（需封装 Electron/Tauri 才做到系统级，见路线图 Phase 3）。
- **云端同步 LWW 最后写入覆盖**：单用户两设备场景下少数覆盖多数可接受；非多人协作冲突解决。
- **移动端隐藏文档输出入口**（依赖本地桥+WPS COM，手机不可用）。
- **构建产物在 `dist2`**：因安全删除钩子会拦截 `dist` 清空，故 `emptyOutDir:false` + 手动维护 dist2。

---

## 8. 审阅提示（给你）

请重点回答：
1. §6 里哪几条「多余/可疑关联」同意删/改？（尤其 #1 孤儿 Dashboard、#2 遗留 JSON）
2. §5.3 / §5.5 里 clean 皮肤的 Bento 重排你是否喜欢？不喜欢就点「重置为初始样式」回退，我再按你新方向改。
3. §5.6 的卡片材质循环是否保留？
4. 深色/浅色两个默认皮肤我一直未动，确认保持冻结。
